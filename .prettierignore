node_modules
package-lock.json
.git
# Not redundant with the generated `target/classes/static/` below, and not generated: the
# whole build tree has to be excluded, not just the webapp output. `api/` and `gateway/`
# get this line from jhipster:maven's fragment (8.x wrote it as the base template's
# `temporaryDir`); this repo has no Maven, so nothing upstream ever writes it here. Since
# .lintstagedrc.cjs widened to `{,**/}*` it is what keeps Istanbul's vendored `prettify.js`
# and friends under `target/test-results/` out of scope when Prettier is invoked with an
# explicit `--ignore-path .prettierignore` — the common lint-staged and CI recipe, and the
# one case where Prettier 3's default inclusion of .gitignore does not cover for us.
target/

# Generated by jhipster:client
target/classes/static/
